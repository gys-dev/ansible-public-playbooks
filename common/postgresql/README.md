# PostgreSQL Setup

This component automates the creation of PostgreSQL databases and users.

## 📋 Variables

Define these variables in `vars.yaml`:

| Variable | Description |
| :--- | :--- |
| `postgresql_databases` | List of databases to create (name). |
| `postgresql_users` | List of users to create (name, password). |
