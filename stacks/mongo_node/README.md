# Node.js + MongoDB Stack

A complete environment for Node.js applications with a backend MongoDB database.

## 📋 Variables

Define these variables in `vars/default.yml`:

| Variable | Description | Default |
| :--- | :--- | :--- |
| `node_version` | The version of Node.js to install. | `16` |
| `app_user` | The system user for the application. | `your_app_user` |
| `app_password` | The password for the application user. | `your_app_password` |